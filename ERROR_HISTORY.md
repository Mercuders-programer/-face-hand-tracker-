# AI 오류 이력 (ERROR_HISTORY)

> 이 파일은 AI가 규칙을 어기거나 실수한 사례를 기록합니다.
> 오류 발생 시마다 업데이트하고 GitHub에 push합니다.

---

## [2026-04-06] 규칙 미준수 — PHS_AI_Rule.md 미참조 + 세션 파일 미업데이트

### 오류 유형
프로세스 규칙 위반 (코드 오류 아님)

### 발생 상황
세 번째·네 번째 작업 세션 (영상 분석 패널 내보내기 버튼 추가 / UI 개선)

### 위반한 규칙
| 규칙 | 내용 | 위반 내용 |
|------|------|-----------|
| 규칙 3 | 작업 완료 시마다 `session_YYYY-MM-DD.md` 업데이트 | 세션 종료 후 사용자 지적을 받고서야 뒤늦게 업데이트 |
| 매 세션 필수 | `PHS_AI_Rule.md` 세션 시작 시 반드시 참조 | 세션 시작 시 파일을 읽지 않음 |

### 근본 원인
1. **PHS_AI_Rule.md 미참조**: MEMORY.md에 경로(`/mnt/c/Users/PHS/Desktop/Claude_Code/0_MD/PHS_AI_Rule.md`)가 명시되어 있었으나 세션 시작 시 파일을 읽지 않았음. 중간에 읽으려 했을 때 경로 오류가 발생했으나 재시도하거나 사용자에게 확인하지 않고 넘어감.
2. **세션 파일 미업데이트**: 규칙을 인지하고 있었음에도 각 작업 완료 후 즉시 기록하지 않고 마지막까지 미룸.

### 재발 방지 대책
- 세션 시작 시 `PHS_AI_Rule.md` → `session_YYYY-MM-DD.md` 순서로 반드시 먼저 읽기
- 작업 하나가 완료될 때마다 즉시 세션 파일 업데이트 후 다음 작업 진행
- 파일 읽기 오류 발생 시 사용자에게 즉시 보고하고 확인 요청

---

---

## [2026-04-06] 규칙 1 미준수 — 테스트를 문법 검사로 대체

### 오류 유형
프로세스 규칙 위반

### 내용
규칙 1: "기능 구현 후 반드시 테스트 실행, 결과 보고" 를 지키지 못함
- 실제로는 `ast.parse()` 문법 검사만 수행
- "테스트 완료" 또는 "OK"로 표현 → 실제 동작 검증이 아님

### 원인
WSL2 환경에서 GUI + 카메라/영상 앱을 실제 실행할 수 없음
Windows에서만 실제 동작 확인 가능

### 재발 방지
- 문법 검사 후 "문법 확인만 가능, 실제 동작은 Windows에서 직접 확인 필요"라고 명시
- 사용자가 Windows 실행 결과를 알려주면 그 내용을 기반으로 오류 대응
- "테스트 완료"라는 표현 사용 금지, 대신 "문법 검사 OK (실행 테스트 필요)" 로 명시

---

## [2026-04-06] 배치파일 인코딩 오류 — make_exe_file.bat 실행 실패

### 오류 유형
Windows CMD 인코딩 불일치

### 증상
```
'e'은(는) 내부 또는 외부 명령... 아닙니다.
'windowed'은(는) 내부 또는 외부 명령... 아닙니다.
```

### 원인
1. 배치파일을 UTF-8로 저장 → Windows CMD는 CP949(한국어)로 파싱
2. `chcp 65001`은 출력 인코딩만 변경, 파일 파싱에는 무효
3. 한글 문자열이 깨지면서 명령어로 잘못 인식됨
4. PyInstaller 명령의 `^` 줄 이음이 깨진 인코딩으로 인해 실패

### 해결
- 배치파일 내 한글 전부 제거, ASCII 영문으로 교체
- `chcp 65001` 제거
- PyInstaller 명령을 한 줄로 합쳐 `^` 줄 이음 제거

### 재발 방지
- Windows .bat 파일은 한글 사용 금지, ASCII 영문만 사용
- PyInstaller 등 긴 명령은 한 줄로 작성

<!-- 새 오류 발생 시 위 블록과 동일한 형식으로 최신 항목을 맨 위에 추가 -->
