# 세션 기록 2026-05-05

## 테스트 결과 (2026-05-05)
- [x] InsightFace buffalo_sc 정상 로드 (CPUExecutionProvider)
- [x] picture_Face.mp4: 87프레임 중 43프레임 감지 (49.4%, 최대 score=0.777) — 이전 세션과 동일
- [x] app.py 정상 실행 확인 (WSL2, 에러 없음)
- [ ] Male Walk.mp4 측면 얼굴 감지 확인 — 파일 없음 (Windows 어딘가에 있을 것)
- note: picture_Face1.mp4는 얼굴 det_score 최대 0.17 (min_conf=0.3 미달로 감지 안됨)

## Windows InsightFace 설치 (2026-05-05)
- Windows Python 3.14.3에 insightface 미설치 → 오류 발생
- `python.exe -m pip install insightface onnxruntime --user --ignore-installed opencv-python-headless` 로 설치 완료
- buffalo_sc 모델 Windows ~/.insightface/models/ 자동 다운로드 완료
- Windows 테스트: picture_Face.mp4 87프레임, 43 감지 (49.4%) ✅

## MAR(입벌림) 감지 복원 (2026-05-05)
- 원인: InsightFace buffalo_sc 5-point kps → lm[13]=lm[14]=왼입꼬리 → MAR 항상 0
- 해결: `src/insightface_detector.py`에 MediaPipe FaceLandmarker 병렬 실행 추가
  - `_get_mp_face()` 싱글턴, `_get_mp_lm()` 헬퍼 추가
  - `detect()` 끝에서 InsightFace 결과의 lm[13]/lm[14]를 MediaPipe 값으로 교체
  - MediaPipe 실패(측면 얼굴 등) 시 MAR=0 유지 (예외 안전 처리)
- 테스트 결과 (WSL2, LD_LIBRARY_PATH 설정):
  - picture_Face.mp4: 87프레임, 43 감지, MAR>0.001: 34프레임 ✅ (이전: 0)

## 영상 저장 오류 수정 (2026-05-05)
- 원인: InsightFace 교체 후 `self._face_det = None` 고정 → 영상 저장 시 "MediaPipe 초기화 실패" 오류
- 수정: `video_panel.py` 2곳에서 `self._face_det is None` → `self._hand_det is None`으로 변경

## 얼굴 이미지 피벗 포인트 + 회전 슬라이더 (2026-05-05)
- `_apply_face_img_overlay()` 함수: `pivot`, `rotation_offset` 파라미터 추가
  - Affine 모드: pivot이 있으면 pivot 좌표를 회전 중심으로 사용, rotation_offset 추가
  - Homography 모드: rotation_offset을 눈 중심 기준 추가 회전으로 적용
- 상태 변수 추가: `_face_pivot`, `_face_rot_var`
- UI 추가: "⊕ 피벗 설정" 버튼, "추가 회전 (°)" 슬라이더 (-180~180)
- `_open_face_pivot_picker()`: 이미지 클릭 팝업, 빨간 십자 마커, 리셋/취소/확인 버튼
- video_panel.py, camera_panel.py 양쪽 모두 적용

## 슬라이더 즉시 렌더링 (2026-05-05)
- `video_panel.py` `__init__` 끝에 `trace_add` 블록 추가
- 얼굴/팔/다리/몸 관련 슬라이더 변수 25개: 값 변경 시 `_refresh_frame()` 자동 호출
- `camera_panel.py`는 `_update_loop`이 매 프레임 변수를 읽으므로 변경 불필요

## 작업 예정
- Idea_Note.txt 항목: 신발/장갑 이미지 추가, 몸 이미지 추가, 손 무기 추가
