# 세션 요약 — 2026-04-10

## 이번 세션에서 한 작업

### 1. camera_panel.py에 HQ 만화 손 스타일 추가

#### 배경
- 이전 세션에서 video_panel.py는 `_draw_cartoon_hands_hq()` (고품질 버전)으로 업그레이드됐으나
- camera_panel.py에서는 만화 손 기능이 제거된 채로 커밋되지 않은 상태로 남아 있었음

#### 수정 내용: `src/camera_panel.py`
- `_HAND_BONES` 상수 추가 (뼈대 연결 21점)
- `_draw_cartoon_hands_hq()` 함수 추가:
  - Pass 1: 외곽 윤곽선 (뼈대 + 관절)
  - Pass 2: 손바닥 볼록껍질(convex hull) 채우기
  - Pass 3: 뼈대 선 + 중심 하이라이트
  - Pass 4: 관절 구체 3단 셰이딩 (shadow / mid / light)
  - Pass 5: 손가락 끝 손톱 + 광택
- `_show_cartoon_hands = tk.BooleanVar(value=False)` 추가
- "└ 만화 손 스타일" 체크박스 추가 (얼굴 모자이크 아래)
- `_overlay_on` 조건에 `or self._show_cartoon_hands.get()` 추가
- `hand_det.detect()` 호출 조건에 `or self._show_cartoon_hands.get()` 추가
- 손 렌더링 로직: `if cartoon → _draw_cartoon_hands_hq / elif hands → mp_draw` 구조로 변경

#### 테스트 결과
- AST 문법 검사: camera_panel.py OK / video_panel.py OK
- WSL2 실행: `LD_LIBRARY_PATH=./lib python3 app.py` → 10초 정상 실행 (exit 124)
- ⚠️ 실제 손 있는 영상으로 시각 확인은 Windows에서 필요

---

### 2. ControlNet + SD 기반 AI 만화 손 내보내기 구현

#### 방향
- 방향 1 (ControlNet + Stable Diffusion) 선택
- 실시간 카메라에는 적용 안 함
- "만화 손 스타일" 체크박스 ON 상태에서 "영상 저장" 시 프레임별 AI 렌더링

#### 신규 파일: `src/cartoon_hand_ai.py`
- `CartoonHandAI` 클래스
- `load(progress_cb)`: ControlNet + SD 파이프라인 초기화 (클래스 캐시)
- `landmarks_to_skeleton(...)`: MediaPipe 21점 → OpenPose hand skeleton 이미지 (512×512)
- `render_frame(frame, hand_res)`: 손 영역 AI 렌더링 후 합성
  - 모든 손 → 단일 바운딩박스 + 정사각형화
  - ControlNet 추론 (기본 20 steps, guidance 7.5, scale 0.85)
  - 손 마스크 (convex hull + dilate + Gaussian blur) 로 부드럽게 합성

#### 수정 파일: `src/video_panel.py`
- `_apply_overlay(bgr, playback, ext_hand_res=None)` 파라미터 추가
  - `ext_hand_res` 제공 시 내부 hand 감지 스킵 + 만화 손 그리기 스킵
- `_export_video()`:
  - `use_ai_hand` 플래그 추가
  - diffusers 미설치 시 설치 안내 에러
  - AI 모드 시 소요 시간 안내 확인 다이얼로그
- `_save_video_frames(path, with_overlay, use_ai_hand=False)`:
  - AI 모드 시 `CartoonHandAI` 초기화 + 별도 HandLandmarker 생성
  - 프레임별: 손 감지 → `_apply_overlay(ext_hand_res=...)` → AI 렌더링
  - 진행 표시: "AI 렌더링 중... 45/300 (0.8초/프레임)"

#### 사용 모델 (자동 다운로드)
| 모델 | 경로 | 크기 |
|------|------|------|
| ControlNet openpose | lllyasviel/control_v11p_sd15_openpose | ~1.4GB |
| SD 만화 스타일 | andite/anything-v4.0 | ~4GB |

#### 테스트 결과
- AST 문법 검사: cartoon_hand_ai.py / video_panel.py / camera_panel.py 모두 OK
- WSL2 실행: 10초 정상 실행 (exit 124)
- ⚠ 실제 AI 렌더링 테스트는 Windows에서 diffusers 설치 후 필요

---

### 3. 영상 분석 뷰 만화 손 실시간 렌더링 제거

#### 변경 내용: `src/video_panel.py`
- `trace_add` 목록에서 `self._show_cartoon_hands` 제거 → 체크박스 ON/OFF가 실시간 뷰를 갱신하지 않음
- `_display_frame` 조건에서 `or self._show_cartoon_hands.get()` 제거 → 체크박스 상태가 오버레이 적용 여부에 영향 없음
- `_apply_overlay`에서 만화 손 HQ 드로잉 블록 제거 → 실시간 뷰에서 `_draw_cartoon_hands_hq()` 미호출

체크박스 역할 변경:
- 이전: 실시간 뷰에서 HQ 만화 손 오버레이 표시
- 이후: 영상 저장 시 AI 렌더링 적용 여부 결정 (내보내기 전용 플래그)

#### 테스트
- AST 문법 검사: OK
- WSL2 실행: 10초 정상 실행 (exit 124)

---

### 4. 얼굴 이미지 오버레이 눈 위치 X 슬라이더 추가

#### 변경 내용: `src/camera_panel.py`, `src/video_panel.py`
- `_apply_face_img_overlay()` 시그니처에 `eye_x_pct=50` 파라미터 추가
- Affine 자동 모드에서 `src_cx = img_w / 2.0` → `src_cx = img_w * (eye_x_pct / 100.0)` 변경
- `self._eye_x_var = tk.IntVar(value=50)` 추가 (양쪽 패널)
- "눈 위치 X (%)" 슬라이더 (범위 10~90) 를 Y 슬라이더 바로 아래 추가
- 호출부에 `eye_x_pct=self._eye_x_var.get()` 전달

#### 테스트
- AST 문법 검사: OK
- WSL2 실행: 10초 정상 실행 (exit 124)

---

### 5. 방향 A — OpenCV 셀 셰이딩 만화 손 렌더러 전면 교체 (2026-04-12)

#### 배경
- ControlNet + SD 파이프라인 → 속도 느림 (250ms~), API 401 오류, 품질 불안정
- 방향 A 선택: OpenCV 기반 셀 셰이딩 렌더러로 완전 교체

#### 신규 파일: `src/hand_renderer.py`
- 7-패스 셀 셰이딩 알고리즘:
  - Pass 0: 검정 외곽선 (두꺼운 캡슐)
  - Pass 1: 그림자 캡슐 (우하단 오프셋)
  - Pass 2: 기본 피부색 (손바닥 hull + 손가락 캡슐)
  - Pass 3: 하이라이트 (좌상단 오프셋, 얇게)
  - Pass 4: 관절 크리즈 링
  - Pass 5: 손바닥 외곽선
  - Pass 6: 손톱 (방향 타원 + 광택)
- `SKIN_PALETTES` (light/medium/dark/anime), `SKIN_LABELS` 공개

#### 수정: `src/camera_panel.py`, `src/video_panel.py`
- 기존 `_HAND_BONES` + `_draw_cartoon_hands_hq()` 제거
- `hand_renderer.draw_cartoon_hands_hq` / `SKIN_LABELS` import 추가
- `_skin_tone_var = tk.StringVar(value="light")` 추가
- 피부톤 라디오 버튼 UI 추가 (밝음/중간/어두움/애니)
- video_panel: `_ai_quality_var` 제거, `_show_cartoon_hands` 체크박스 레이블에서 "(내보내기 AI)" 제거
- video_panel: `_display_frame` 조건에 `_show_cartoon_hands` 추가 (실시간 뷰도 표시)
- video_panel: `_apply_overlay()` 에 만화 손 그리기 추가
- video_panel: `_export_video()` AI 관련 코드 전부 제거, 단순화
- video_panel: `_save_video_frames()` — CartoonHandAI 제거, 풀 해상도 HandLandmarker + draw_cartoon_hands_hq로 교체

#### 속도 비교
- 이전 (LCM 4스텝): 250ms/프레임
- 이후 (OpenCV):     4.2ms/프레임 (약 60배 향상)

#### 테스트
- AST 문법 검사: hand_renderer / camera_panel / video_panel 모두 OK
- hand_renderer 기능 테스트: 4 스킨톤 렌더링 각 ~4ms, 픽셀 정상 출력
- WSL2 app.py 실행: 10초 정상 실행 (exit 124)

---

## 환경 정보
- OS: Windows 11 + WSL2 (Ubuntu)
- GPU: RTX 5070 Laptop (8GB VRAM), CUDA 12.9
- Python (WSL2): 3.12, mediapipe 0.10.33, opencv 4.13.0, Pillow 12.1.1
