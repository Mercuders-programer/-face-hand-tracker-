# 세션 2026-04-23

## 작업: 얼굴 이미지 입 벌림/닫힘 자동 전환 (MAR 기반)

### 구현 내용

**수정 파일:** `src/camera_panel.py`, `src/video_panel.py`

#### 변경 사항 (두 파일 공통)

1. **`_compute_mar()` 모듈 함수 추가** (`_apply_face_img_overlay` 바로 위)
   - Mouth Aspect Ratio = `|lf[14].y - lf[13].y| * h / (|lf[263].x - lf[33].x| * w)`
   - 입 닫힘 ~0.04, 입 벌림 ~0.15+

2. **`__init__` 상태 변수 추가**
   - `self._face_img_open = None` — 입 벌림 이미지 (BGRA)
   - `self._face_img_open_pts = None` — 소스 키포인트
   - `self._mouth_thr_var = tk.DoubleVar(value=0.12)` — 전환 임계값

3. **UI 추가** (얼굴 이미지 섹션 내 EMA 슬라이더 아래)
   - 구분선
   - "🖼  입 벌림 이미지 로드" 버튼 (`self._face_img_open_btn`)
   - 파일명 라벨 (`self._face_img_open_lbl`)
   - "전환 임계값 (MAR)" 슬라이더 (0.02~0.30, 기본 0.12, 초기 DISABLED)
   - 입 벌림 이미지 로드 시 슬라이더 활성화

4. **`_toggle_face_image_open()` / `_load_face_image_open()` 메서드 추가**
   - 기존 `_toggle_face_image` / `_load_face_image` 패턴 그대로
   - video_panel만: 토글/로드 후 `self._det_cache = None; self._refresh_frame()`

5. **렌더링 루프 수정**
   ```python
   if _fi is not None:
       if self._face_img_open is not None:
           _mar = _compute_mar(face_res, w, h)
           if _mar >= self._mouth_thr_var.get():
               _fi = self._face_img_open
               _fp = self._face_img_open_pts
       _apply_face_img_overlay(...)
   ```

### 테스트 결과
- 문법 검사: OK (ast.parse)
- 심볼 확인: 모든 신규 심볼 존재 확인

### 상태
- 구현 완료, GUI 실행 테스트 대기 (사용자 확인 필요)
