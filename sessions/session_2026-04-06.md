# 세션 요약 — 2026-04-06

## 이번 세션에서 한 작업

### 1. GUI 앱 실행 시도
- `app.py` : Tkinter GUI (PoseTracker 메인 화면)
- WSL2에서 실행 → GUI는 정상 동작, 카메라 없어서 `/dev/video0` 오류
- **결론**: 카메라 추적은 Windows에서 직접 실행해야 함

### 2. Windows Python 환경 설정
- Windows에 Python 미설치 상태였음
- Microsoft Store 앱 실행 별칭 문제 (`python` 명령이 가짜 별칭으로 연결됨)
- **해결**: Windows 설정 → 앱 실행 별칭 → python.exe 끄기 → python.org에서 설치 + PATH 체크
- Windows에 패키지 설치: `pip install mediapipe opencv-python pillow numpy`

### 3. 카메라 화면 미표시 버그 수정 (핵심)

#### 원인 파악 과정
- 상태는 "카메라 실행중"인데 캔버스에 영상 안 나옴
- 테스트 스크립트(`test_pipeline.py`) 실행 → **`mediapipe 0.10.33`에서 `mp.solutions` API 완전 제거** 확인
  ```
  AttributeError: module 'mediapipe' has no attribute 'solutions'
  ```

#### 수정 내용

**`src/tracker.py` 전면 재작성**
- 구 API: `mp.solutions.face_mesh.FaceMesh()`, `mp.solutions.hands.Hands()`
- 신 API: `mediapipe.tasks.python.vision.FaceLandmarker`, `HandLandmarker`
- 모듈 레벨 헬퍼 함수로 분리: `_extract_face()`, `_extract_hand()`
- 결과 구조 변경: `results.multi_face_landmarks` → `results.face_landmarks`

**`src/camera_panel.py` 수정**
- imports 추가: Tasks API, drawing_utils, drawing_styles, Connections 클래스
- `_capture_loop()` 재작성: 새 Tasks API 사용
- 모델 파일 경로 상수 추가: `FACE_MODEL`, `HAND_MODEL`
- `_collect_frame()` 수정: 새 result 구조에 맞게
- 자동 카메라 시작: `win.after(200, self._start_camera)`

#### 모델 파일 다운로드
```
models/face_landmarker.task  (3.6 MB)
models/hand_landmarker.task  (7.6 MB)
```

### 4. AI 규칙 파일 설정
- `PHS_AI_Rule.md` 생성 (프로젝트 루트)
- 규칙 1: 기능 구현 후 테스트 의무화
- 규칙 2: 오류/에러 발생 시 사용자에게 먼저 물어보고 수정
- 작업 순서: 구현 → 테스트 → (오류 시) 사용자 확인 후 수정

### 5. 랜드마크 표시 토글 체크박스 추가
- `src/camera_panel.py` 우측 컨트롤 패널에 "랜드마크 표시" 체크박스 추가
- 체크 ON: 얼굴 윤곽선 + 손 랜드마크 실시간 표시
- 체크 OFF: 원본 영상만 표시 (랜드마크 숨김)
- `_show_overlay = tk.BooleanVar(value=True)` 로 상태 관리

### 6. sessions 폴더 생성 (Windows 접근 가능)
- 위치: `C:\Users\PHS\Desktop\Claude_Code\my_openPose_proj\sessions\`
- 앞으로 세션별 대화 요약을 이 폴더에 저장
- 대화 원본(jsonl)은 WSL2 경로에만 존재 → Windows 탐색기 검색 불가

### 7. Windows CMD 실행 명령어
```cmd
cd C:\Users\PHS\Desktop\Claude_Code\my_openPose_proj
python app.py
```
최초 실행 전 패키지 설치:
```cmd
pip install mediapipe opencv-python pillow numpy
```

### 8. AI 규칙 파일 위치 정리
- `PHS_AI_Rule.md` → 프로젝트 루트 (Windows에서 바로 접근 가능)
- 내용: 구현→테스트→(오류 시)사용자 확인 순서로 작업

---

## 현재 상태

### 파일 구조
```
app.py                        — Tkinter 메인 GUI
src/camera_panel.py           — 카메라 추적 패널 (랜드마크 토글 체크박스 추가)
src/tracker.py                — MediaPipe Tasks API 래퍼
src/exporter.py               — JSON + AE 키프레임 내보내기
models/face_landmarker.task   — FaceLandmarker 모델 (3.6MB)
models/hand_landmarker.task   — HandLandmarker 모델 (7.6MB)
test_pipeline.py              — 파이프라인 테스트 스크립트
PHS_AI_Rule.md                — AI 행동 규칙 파일
sessions/                     — 세션별 대화 요약 폴더 (Windows 접근 가능)
```

### 9. 영상 분석 패널 구현 (두 번째 세션)

#### 수정 파일
- **`app.py`**: "영상 분석" 버튼 활성화 (BTN_DIM→ACCENT, command=_open_video) + `_open_video()` 메서드 추가
- **`src/video_panel.py`** 신규 생성 (270줄)

#### VideoPanel 기능
- `cv2.VideoCapture`로 파일 열기 (mp4/avi/mov/mkv)
- 영상 캔버스 (fill=BOTH, expand=True) + PIL ImageTk 표시
- 타임라인 Canvas (h=30): 클릭/드래그 스크러빙, ACCENT 진행선 + 흰 원형 플레이헤드
- 컨트롤 바: ▶ 재생 / ⏸ 일시정지 버튼, 시간 표시, 랜드마크 체크박스
- 랜드마크 체크박스: `trace_add("write")` → 체크 즉시 현재 프레임 재표시
- MediaPipe FaceLandmarker + HandLandmarker (IMAGE 모드, camera_panel.py와 동일 패턴)
- `_on_close`: cap + detector 모두 정리

#### 테스트 결과
- 문법 검사 (AST parse): app.py / src/video_panel.py 모두 OK
- VideoPanel 메서드 18개 + fmt 내부 함수 확인
- MainApp에 `_open_video` 정상 등록 확인
- ⚠️ 실제 GUI 실행 테스트는 Windows에서 직접 수행 필요

### 미완료 / 다음 할 일
- [ ] Windows에서 `python test_pipeline.py` 실행해서 파이프라인 통과 확인
- [ ] `python app.py` → "영상 분석" 버튼 클릭 → MP4 파일 선택 → 재생 확인
- [ ] 타임라인 클릭/드래그 스크러빙 동작 확인
- [ ] 랜드마크 체크박스 ON/OFF 즉시 반영 확인
- [ ] 카메라 추적 → 녹화 → AE 내보내기 전체 플로우 테스트

---

## 환경 정보
- OS: Windows 11 + WSL2 (Ubuntu)
- GPU: RTX 5070 Laptop (8GB VRAM), CUDA 12.9
- Python (WSL2): 3.12, mediapipe 0.10.33, opencv 4.13.0, Pillow 12.1.1
- Python (Windows): python.org에서 별도 설치
